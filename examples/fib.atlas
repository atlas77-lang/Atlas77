import "std/io"

func main()  {
    let fib_80 = iterative_fib(80);
    println(-fib_80 as str);
    println(-1 as str);
}

func recursive_fib(n: i64) -> i64 {
    if n <= 1 {
        return n;
    } else {
        return recursive_fib(n - 1) + recursive_fib(n - 2);
    }
}

func iterative_fib(n: i64) -> i64 {
    let a = 0;
    let b = 1;
    let i = 0;
    while i < n {
        let temp = a;
        a = b;
        b = temp + b;
        i = i + 1;
    }
    return a;
}
