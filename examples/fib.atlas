func main() -> f64 {
    let tmp: i64 = fib(20.0);
    return fib(20.0);
}

func fib(n: f64) -> f64 {
    if n <= 1.0 {
        return n;
    } else {
        return fib(n - 1.0) + fib(n - 2.0);
    }
}