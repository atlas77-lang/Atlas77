// The program will output:
// No stack frame to return to Ok(VMData { tag: 8(i64), data: 25}) @ 2
// Because entering main doesn't create a stack frame

func main() -> i64 {
    return square(5);
}

func square(x: i64) -> i64 {
    return x * x;
}
